# Project Index

> **Auto-generated by `npm run index`. Do not edit by hand.**
> Last regenerated: 2026-05-20T15:46:10.494Z
>
> Claude reads this file BEFORE searching the codebase. If something needed
> isn't listed here, it doesn't exist yet — create it per CLAUDE.md.

## components/common/

### Alert `@/components/common`
```ts
{
  variant:  AlertVariant;
  children: ReactNode;
}
```
<sub>src/components/common/Alert/Alert.tsx</sub>

### Badge `@/components/common`
```ts
{
  children: ReactNode;
  color?: string;
  className?: string;
}
```
<sub>src/components/common/Badge/Badge.tsx</sub>

### Button `@/components/common`
```ts
{
  variant?: 'primary' | 'secondary' | 'ghost';
  fullWidth?: boolean;
  leftIcon?: ReactNode;
  children: ReactNode;
}
```
<sub>src/components/common/Button/Button.tsx</sub>

### Card `@/components/common`
```ts
{
  children: ReactNode;
  className?: string;
}
```
<sub>src/components/common/Card/Card.tsx</sub>

### DateInput `@/components/common`
```ts
{
  value:    string;                              // ISO YYYY-MM-DD
  onChange: (next: string) => void;
  min?:     string;
  max?:     string;
  ariaLabel?: string;
}
```
<sub>src/components/common/DateInput/DateInput.tsx</sub>

### Drawer `@/components/common`
```ts
{
  open:         boolean;
  onOpenChange: (open: boolean) => void;
  side?:        'left' | 'right';
  /** Override the default `widthClamp` style. */
  widthStyle?:  React.CSSProperties['width'];
  /** Accessible label — read by screen readers as the dialog name. */
  ariaLabel?:   string;
  children:     React.ReactNode;
}
```
<sub>src/components/common/Drawer/Drawer.tsx</sub>

### DrawerHeader `@/components/common`
_props: see source_
<sub>src/components/common/Drawer/Drawer.tsx</sub>

### DrawerTitle `@/components/common`
_props: see source_
<sub>src/components/common/Drawer/Drawer.tsx</sub>

### DrawerClose `@/components/common`
Visible close button (the × in the header). Keyboard Escape works regardless.

_props: see source_
<sub>src/components/common/Drawer/Drawer.tsx</sub>

### DrawerBody `@/components/common`
_props: see source_
<sub>src/components/common/Drawer/Drawer.tsx</sub>

### DrawerFooter `@/components/common`
_props: see source_
<sub>src/components/common/Drawer/Drawer.tsx</sub>

### PanelErrorState `@/components/common`
```ts
{
  panelName?: string;
  onRetry?:   () => void;
}
```
<sub>src/components/common/ErrorBoundary/PanelErrorState.tsx</sub>

### FileDropzone `@/components/common`
```ts
{
  value:    UploadedDocument[];
  onChange: (docs: UploadedDocument[]) => void;
  accept?:  string;
}
```
<sub>src/components/common/FileDropzone/FileDropzone.tsx</sub>

### FormField `@/components/common`
```ts
{
  label:           string;
  error?:          string;
  /** Renders a red asterisk after the label when true. */
  required?:       boolean;
  /** Helper text shown below the field (suppressed when `error` is set). */
  helper?:         ReactNode;
  /** When true, helper renders in `colors.greenAssigned` (e.g. "Auto-filled below"). */
  helperVariant?:  'neutral' | 'success';
  labelClassName?: string;
  children:        ReactNode;
}
```
<sub>src/components/common/FormField/FormField.tsx</sub>

### Input `@/components/common`
```ts
{
  /**
  * `'legacy'` (default) — original Tailwind utility-class look. Used by
  * dashboard / login forms. Keeps existing visuals untouched.
  * `'tokenized'` — N3+ tokenised chrome (height 38.8, slate300 0.8 border,
  * sharp corners, padding 9/11). Use this in N-track form sections.
  */
  variant?:    'legacy' | 'tokenized';
  /** Render a left-icon prefix slot (icon node positioned absolutely). */
  leftIcon?:   React.ReactNode;
  /** Render a right-icon slot (positioned absolutely). */
  rightIcon?:  React.ReactNode;
  /** Visually mute the value (used when "Auto-filled from contact"). */
  mutedValue?: boolean;
}
```
<sub>src/components/common/Input/Input.tsx</sub>

### LabelValueRow `@/components/common`
```ts
{
  label: string;
  value: ReactNode;
}
```
<sub>src/components/common/LabelValueRow/LabelValueRow.tsx</sub>

### MultiSelect `@/components/common`
```ts
{
  value:        TId[];
  onChange:     (next: TId[]) => void;
  options:      MultiSelectOption<TId>[];
  placeholder?: string;
  ariaLabel?:   string;
}
```
<sub>src/components/common/MultiSelect/MultiSelect.tsx</sub>

### Pagination `@/components/common`
S4 — left side: "Showing X – Y of Z items" + divider + "Page A of B". Right side: chevron prev / numbered pages / chevron next. All values read from `paginationStyles`. No invented properties — Figma values verified against node 520-29725.

```ts
{
  page:          number;
  pageSize:      number;
  total:         number;
  onPageChange:  (page: number) => void;
  itemLabel?:    string;
}
```
<sub>src/components/common/Pagination/Pagination.tsx</sub>

### PasswordInput `@/components/common`
_props: see source_
<sub>src/components/common/PasswordInput/PasswordInput.tsx</sub>

### RequiredFieldsCounter `@/components/common`
```ts
{
  completed: number;
  total:     number;
}
```
<sub>src/components/common/RequiredFieldsCounter/RequiredFieldsCounter.tsx</sub>

### SectionPanel `@/components/common`
```ts
{
  title:      string;
  badge?:     number;
  action?:    ReactNode;
  children:   ReactNode;
  className?: string;
}
```
<sub>src/components/common/SectionPanel/SectionPanel.tsx</sub>

### SegmentedTabs `@/components/common`
```ts
{
  tabs: ReadonlyArray<Tab<T>>;
  activeValue: T;
  onChange: (value: T) => void;
  size?: 'sm' | 'md';
}
```
<sub>src/components/common/SegmentedTabs/SegmentedTabs.tsx</sub>

### Select `@/components/common`
```ts
{
  value:           TId | null;
  onChange:        (id: TId | null) => void;
  options:         SelectOption<TId>[];
  placeholder?:    string;
  leftIcon?:       ReactNode;
  disabled?:       boolean;
  renderOption?:   (opt: SelectOption<TId>) => ReactNode;
  ariaLabel?:      string;
}
```
<sub>src/components/common/Select/Select.tsx</sub>

### SortableColumnHeader `@/components/common`
Sortable column header with chevron indicator. When the column is the active sort column the chevron points in the direction of sort (Down = desc, Up = asc); otherwise the inactive chevron is the lucide `ChevronsUpDown` "double-chevron". Glyph identification is a best-guess — Figma JSON does not expose vector path data for header icons. Verify visually and update the imported lucide icons if wrong.

```ts
{
  label:        string;
  sortKey?:     string;
  currentField?: string;
  currentDirection?: 'asc' | 'desc';
  onSortChange?: (field: string) => void;
  align?:       'left' | 'right' | 'center';
}
```
<sub>src/components/common/SortableColumnHeader/SortableColumnHeader.tsx</sub>

### Spinner `@/components/common`
_props: see source_
<sub>src/components/common/Spinner/Spinner.tsx</sub>

### StageProgress `@/components/common`
```ts
{
  currentStage:        SubmissionStage;
  completedFields:     SubmissionRequiredField[];
  totalRequiredFields: number;
}
```
<sub>src/components/common/StageProgress/StageProgress.tsx</sub>

### Tabs `@/components/common`
```ts
{
  value: T;
  onChange: (v: T) => void;
  options: TabOption<T>[];
}
```
<sub>src/components/common/Tabs/Tabs.tsx</sub>


## components/domain/

### AccountIdentitySection `@/components/domain`
_props: see source_
<sub>src/components/domain/AccountIdentitySection/AccountIdentitySection.tsx</sub>

### AccountSearchInput `@/components/domain`
```ts
{
  value:     AccountLookup | null;
  onChange:  (account: AccountLookup | null) => void;
  ariaLabel?: string;
}
```
<sub>src/components/domain/AccountSearchInput/AccountSearchInput.tsx</sub>

### AiAutoFillSection `@/components/domain`
_props: see source_
<sub>src/components/domain/AiAutoFillSection/AiAutoFillSection.tsx</sub>

### AlertFilterTabs `@/components/domain`
```ts
{
  activeFilter:   AlertFilter;
  onFilterChange: (next: AlertFilter) => void;
}
```
<sub>src/components/domain/AlertFilterTabs/AlertFilterTabs.tsx</sub>

### AlertRow `@/components/domain`
```ts
{
  alert:            Alert;
  displayTimestamp: string;
}
```
<sub>src/components/domain/AlertRow/AlertRow.tsx</sub>

### AlertsPanel `@/components/domain`
```ts
{
  alerts:             Alert[];
  alertTimestamps:    Record<string, string>;
  activeFilter:       AlertFilter;
  onFilterChange:     (next: AlertFilter) => void;
  totalCriticalCount: number;
  totalWarningCount:  number;
  onViewAllClick?:    () => void;
}
```
<sub>src/components/domain/AlertsPanel/AlertsPanel.tsx</sub>

### AppetiteBar `@/components/domain`
Track + fill + percent label. Fill color flips green↔amber at the threshold defined in `appetiteBarStyles.greenThreshold` (Figma sample shows ≥85% green, <85% amber — the helper isolates the threshold so components don't repeat it). NO border radius — Figma absent.

```ts
{
  percent: number;
}
```
<sub>src/components/domain/AppetiteBar/AppetiteBar.tsx</sub>

### Avatar `@/components/domain`
```ts
{
  name: string;
  role: UnderwriterRole;
  size?: number;
}
```
<sub>src/components/domain/Avatar/Avatar.tsx</sub>

### BrandPanel `@/components/domain`
_props: see source_
<sub>src/components/domain/BrandPanel/BrandPanel.tsx</sub>

### BrokerContactsSection `@/components/domain`
_props: see source_
<sub>src/components/domain/BrokerContactsSection/BrokerContactsSection.tsx</sub>

### CardFooter `@/components/domain`
```ts
{
  left: ReactNode;
  right?: ReactNode;
}
```
<sub>src/components/domain/CardShell/CardShell.tsx</sub>

### CardShell `@/components/domain`
```ts
{
  title: string;
  icon: ReactNode;
  width?: number;
  iconColor?: string;
  headerRight?: ReactNode;
  footer?: ReactNode;
  children: ReactNode;
}
```
<sub>src/components/domain/CardShell/CardShell.tsx</sub>

### ChartLegend `@/components/domain`
```ts
{
  items: ChartLegendItem[];
}
```
<sub>src/components/domain/ChartLegend/ChartLegend.tsx</sub>

### CountBadge `@/components/domain`
```ts
{
  count: number;
  hideWhenZero?: boolean;
}
```
<sub>src/components/domain/CountBadge/CountBadge.tsx</sub>

### EmptyState `@/components/domain`
```ts
{
  message: string;
}
```
<sub>src/components/domain/EmptyState/EmptyState.tsx</sub>

### KpiCard `@/components/domain`
```ts
{
  label: string;
  value: ReactNode;
  sub?: string;
  trend?: string;
  icon?: ReactNode;
  trendPositive?: boolean;
  accentColor?: string;
}
```
<sub>src/components/domain/KpiCard/KpiCard.tsx</sub>

### MemberBrokerTab `@/components/domain`
```ts
{
  member:       MemberDetail;
  brokerage:    BrokerageDetail;
  contacts:     BrokerContact[];
  activeView:   'member' | 'brokerage';
  onViewChange: (v: 'member' | 'brokerage') => void;
}
```
<sub>src/components/domain/MemberBrokerTab/MemberBrokerTab.tsx</sub>

### NewSubmissionActions `@/components/domain`
```ts
{
  validationMessage: string | null;
  isSubmitBlocked:   boolean;
  submitError:       string | null;
  isSubmitting:      boolean;
  onCreateClick:     () => void;
  onCancelClick:     () => void;
}
```
<sub>src/components/domain/NewSubmissionActions/NewSubmissionActions.tsx</sub>

### NewSubmissionForm `@/components/domain`
_props: see source_
<sub>src/components/domain/NewSubmissionForm/NewSubmissionForm.tsx</sub>

### NewSubmissionHeader `@/components/domain`
```ts
{
  generatedName: string | null;
}
```
<sub>src/components/domain/NewSubmissionHeader/NewSubmissionHeader.tsx</sub>

### NewSubmissionSidebar `@/components/domain`
_props: see source_
<sub>src/components/domain/NewSubmissionSidebar/NewSubmissionSidebar.tsx</sub>

### NotesSection `@/components/domain`
_props: see source_
<sub>src/components/domain/NotesSection/NotesSection.tsx</sub>

### OpenTasksPanel `@/components/domain`
```ts
{
  tasks: Task[];
  totalCount: number;
  overdueCount: number;
  activeFilter: TaskFilter;
  onFilterChange: (f: TaskFilter) => void;
  onTaskClick?: (task: Task) => void;
  onViewAll?: () => void;
}
```
<sub>src/components/domain/OpenTasksPanel/OpenTasksPanel.tsx</sub>

### OverviewTab `@/components/domain`
_props: see source_
<sub>src/components/domain/OverviewTab/OverviewTab.tsx</sub>

### PipelineBarChart `@/components/domain`
```ts
{
  months:      PipelineMonthBucket[];
  series:      readonly PipelineSeriesKey[];
  onBarClick?: (monthIso: string, seriesKey: PipelineSeriesKey) => void;
}
```
<sub>src/components/domain/PipelineBarChart/PipelineBarChart.tsx</sub>

### PipelinePanel `@/components/domain`
```ts
{
  months:      PipelineMonthBucket[];
  onBarClick?: (monthIso: string, seriesKey: PipelineSeriesKey) => void;
}
```
<sub>src/components/domain/PipelinePanel/PipelinePanel.tsx</sub>

### PolicyDatesSection `@/components/domain`
_props: see source_
<sub>src/components/domain/PolicyDatesSection/PolicyDatesSection.tsx</sub>

### PortfolioSnapshotPanel `@/components/domain`
```ts
{
  stats:        PortfolioStat[];
  onStatClick?: (key: PortfolioStatKey) => void;
}
```
<sub>src/components/domain/PortfolioSnapshotPanel/PortfolioSnapshotPanel.tsx</sub>

### PriorityBadge `@/components/domain`
```ts
{
  priority: Priority;
}
```
<sub>src/components/domain/PriorityBadge/PriorityBadge.tsx</sub>

### PriorityChip `@/components/domain`
```ts
{
  priority: SubmissionPriority;
}
```
<sub>src/components/domain/PriorityChip/PriorityChip.tsx</sub>

### ProductChips `@/components/domain`
Renders product-line chips in DISPLAY_ORDER even if input is shuffled. Per-product icon glyphs come from `productLineIcons` (best-guess semantic mapping; Figma JSON did not expose vector data). Chip is translucent blue with brand-blue text + faint blue border. NO border radius — Figma node has no `cornerRadius`.

```ts
{
  products: ProductLine[];
}
```
<sub>src/components/domain/ProductChips/ProductChips.tsx</sub>

### SectionCard `@/components/domain`
```ts
{
  title: string;
  filters?: ReactNode;
  footer?: ReactNode;
  children: ReactNode;
}
```
<sub>src/components/domain/SectionCard/SectionCard.tsx</sub>

### StatRow `@/components/domain`
```ts
{
  stat:    PortfolioStat;
  isLast?: boolean;
  onClick?: (key: PortfolioStatKey) => void;
}
```
<sub>src/components/domain/StatRow/StatRow.tsx</sub>

### StatusBadge `@/components/domain`
```ts
{
  status: SubmissionStatus;
}
```
<sub>src/components/domain/StatusBadge/StatusBadge.tsx</sub>

### StatusPill `@/components/domain`
```ts
{
  status: SubmissionStatus;
}
```
<sub>src/components/domain/StatusPill/StatusPill.tsx</sub>

### SubmissionDocumentsSection `@/components/domain`
```ts
{
  value:    UploadedDocument[];
  onChange: (docs: UploadedDocument[]) => void;
}
```
<sub>src/components/domain/SubmissionDocumentsSection/SubmissionDocumentsSection.tsx</sub>

### SubmissionsFiltersDrawer `@/components/domain`
```ts
{
  filters:               SubmissionsFilterParams;
  onChange:              (next: SubmissionsFilterParams) => void;
  availableBrokers:      string[];
  availableUnderwriters: string[];
}
```
<sub>src/components/domain/SubmissionsFiltersDrawer/SubmissionsFiltersDrawer.tsx</sub>

### SubmissionsHeader `@/components/domain`
```ts
{
  stats?:    SubmissionsHeaderStats;
  isLoading?: boolean;
}
```
<sub>src/components/domain/SubmissionsHeader/SubmissionsHeader.tsx</sub>

### SubmissionsListTable `@/components/domain`
```ts
{
  response?:    SubmissionsListResponse;
  isLoading?:   boolean;
  sort:         SubmissionsSort;
  onSortChange: (next: SubmissionsSort) => void;
  page:         number;
  pageSize:     number;
  onPageChange: (page: number) => void;
  onRowClick?:  (id: string) => void;
}
```
<sub>src/components/domain/SubmissionsListTable/SubmissionsListTable.tsx</sub>

### SubmissionsScopeTabs `@/components/domain`
```ts
{
  scope:              SubmissionsScope;
  onChange:           (scope: SubmissionsScope) => void;
  resultsCount:       number;
  isLoading?:         boolean;
  activeFilterCount?: number;
  onFiltersClick:     () => void;
  filtersOpen?:       boolean;
}
```
<sub>src/components/domain/SubmissionsScopeTabs/SubmissionsScopeTabs.tsx</sub>

### SubmissionsTable `@/components/domain`
```ts
{
  submissions: Submission[];
  scope?: Scope;
  onScopeChange?: (s: Scope) => void;
  filterStatus?: FilterValue;
  onFilterChange?: (f: FilterValue) => void;
  onRowClick?: (s: Submission) => void;
  onViewAll?: () => void;
}
```
<sub>src/components/domain/SubmissionsTable/SubmissionsTable.tsx</sub>

### SubmissionTypeCard `@/components/domain`
```ts
{
  type:     SubmissionType;
  selected: boolean;
  onClick:  () => void;
}
```
<sub>src/components/domain/SubmissionTypeCard/SubmissionTypeCard.tsx</sub>

### TaskRow `@/components/domain`
```ts
{
  task: Task;
  onClick?: (task: Task) => void;
}
```
<sub>src/components/domain/TaskRow/TaskRow.tsx</sub>

### TeamPerformancePanel `@/components/domain`
```ts
{
  underwriters: UnderwriterPerformance[];
}
```
<sub>src/components/domain/TeamPerformancePanel/TeamPerformancePanel.tsx</sub>

### TeamPerformanceRow `@/components/domain`
```ts
{
  underwriter: UnderwriterPerformance;
  isLast?:     boolean;
}
```
<sub>src/components/domain/TeamPerformanceRow/TeamPerformanceRow.tsx</sub>

### UnderwritingTeamSection `@/components/domain`
_props: see source_
<sub>src/components/domain/UnderwritingTeamSection/UnderwritingTeamSection.tsx</sub>


## components/layout/

### AppShell `@/components/layout`
_props: see source_
<sub>src/components/layout/AppShell.tsx</sub>

### PageHeader `@/components/layout`
```ts
{
  title: string;
  subtitle?: string;
  action?: ReactNode;
}
```
<sub>src/components/layout/PageHeader.tsx</sub>

### Sidebar `@/components/layout`
```ts
{
  mobileOpen?: boolean;
  onClose?:    () => void;
}
```
<sub>src/components/layout/Sidebar/Sidebar.tsx</sub>

### TopBar `@/components/layout`
```ts
{
  onMobileMenu?: () => void;
}
```
<sub>src/components/layout/TopBar/TopBar.tsx</sub>


## containers/

### dashboard
- **AlertsPanelContainer** — `src/containers/dashboard/AlertsPanelContainer.tsx`
- **KpiRowContainer** — `src/containers/dashboard/KpiRowContainer.tsx`
- **OpenTasksPanelContainer** — `src/containers/dashboard/OpenTasksPanelContainer.tsx`
- **PipelinePanelContainer** — `src/containers/dashboard/PipelinePanelContainer.tsx`
- **PortfolioSnapshotContainer** — `src/containers/dashboard/PortfolioSnapshotContainer.tsx`
- **SubmissionsTableContainer** — `src/containers/dashboard/SubmissionsTableContainer.tsx`
- **TeamPerformanceContainer** — `src/containers/dashboard/TeamPerformanceContainer.tsx`

### login
- **LoginFormContainer** — `src/containers/login/LoginFormContainer.tsx`

### submissions
- **MemberBrokerTabContainer** — `src/containers/submissions/MemberBrokerTabContainer.tsx`
- **NewSubmissionActionsContainer** — `src/containers/submissions/NewSubmissionActionsContainer.tsx`
- **NewSubmissionFormContainer** — `src/containers/submissions/NewSubmissionFormContainer.tsx`
- **NewSubmissionHeaderContainer** — `src/containers/submissions/NewSubmissionHeaderContainer.tsx`
- **NewSubmissionSidebarContainer** — `src/containers/submissions/NewSubmissionSidebarContainer.tsx`
- **SubmissionsFiltersContainer** — `src/containers/submissions/SubmissionsFiltersContainer.tsx`
- **SubmissionsHeaderContainer** — `src/containers/submissions/SubmissionsHeaderContainer.tsx`
- **SubmissionsListContainer** — `src/containers/submissions/SubmissionsListContainer.tsx`
- **SubmissionsScopeTabsContainer** — `src/containers/submissions/SubmissionsScopeTabsContainer.tsx`


## features/
- **appetite-rules** — _empty_
- **dashboard** — pages: `DashboardPage`
- **inbox** — _empty_
- **login** — pages: `LoginPage`
- **portfolio** — _empty_
- **submissions** — pages: `SubmissionDetailPage`, `SubmissionsNewPage`, `SubmissionsPage`
- **tasks** — _empty_


## hooks/

### auth
- **useAuthUser** `@/hooks/auth`

### common
- **useDebounce** `@/hooks/common`
- **useDisclosure** `@/hooks/common`
- **useSidebarCollapsed** `@/hooks/common`


## services/

### alerts `@/services/alerts`
- queries: `useGetAlertsQuery`
<sub>src/services/alerts/alertsApi.ts</sub>

### dashboard `@/services/dashboard`
- queries: `useGetKpisQuery`
<sub>src/services/dashboard/dashboardApi.ts</sub>

### pipeline `@/services/pipeline`
- queries: `useGetPipelineQuery`
<sub>src/services/pipeline/pipelineApi.ts</sub>

### portfolio `@/services/portfolio`
- queries: `useGetSnapshotQuery`
<sub>src/services/portfolio/portfolioApi.ts</sub>

### submissions `@/services/submissions`
- queries: `useGetSubmissionsQuery`, `useGetSubmissionsHeaderQuery`, `useGetSubmissionsListQuery`, `useSearchAccountsQuery`, `useGetBrokeragesQuery`, `useGetUnderwritersQuery`, `useGetSubmissionDetailQuery`, `useGetCoverageLinesQuery`, `useGetLossHistoryQuery`, `useGetMemberDetailQuery`, `useGetBrokerageDetailQuery`, `useGetBrokerContactsQuery`
- mutations: `useCreateSubmissionMutation`, `useUploadDocumentMutation`
<sub>src/services/submissions/submissionsApi.ts</sub>

### tasks `@/services/tasks`
- queries: `useGetTasksQuery`
<sub>src/services/tasks/tasksApi.ts</sub>

### teamPerformance `@/services/teamPerformance`
- queries: `useGetTeamPerformanceQuery`
<sub>src/services/teamPerformance/teamPerformanceApi.ts</sub>


## store/slices/

### auth `@/store/slices`
- actions: `setCredentials`, `logout`
<sub>src/store/slices/authSlice.ts</sub>


## shared/types `@/shared/types`
`AlertSeverity`, `AlertFilter`, `Alert`, `AlertsResponse`, `Priority`, `DashboardKpis`, `User`, `MemberQuickStats`, `MemberAccountInfo`, `MemberInstitutionProfile`, `AddressBlock`, `MemberAddressInfo`, `MemberDetail`, `BrokerageQuickStats`, `BrokerageAccountInfo`, `BrokerageAddressInfo`, `BrokerageDetail`, `BrokerContact`, `NewSubmissionFormValues`, `SubmissionType`, `SubmissionStage`, `AccountLookup`, `BrokerContactLookup`, `BrokerageLookup`, `UnderwriterLookup`, `UploadedDocument`, `CreateSubmissionPayload`, `CreateSubmissionResponse`, `UploadDocumentPayload`, `UploadDocumentResponse`, `PipelineSeriesKey`, `PipelineMonthBucket`, `Pipeline`, `PortfolioStatKey`, `StatFormat`, `PortfolioStat`, `PortfolioSnapshot`, `SubmissionStatus`, `SubmissionPriority`, `InstitutionType`, `ProductLine`, `Submission`, `SubmissionsScope`, `SubmissionsSortField`, `SubmissionsSortDirection`, `SubmissionsSort`, `SubmissionsFilterParams`, `SubmissionsHeaderStats`, `SubmissionsListQuery`, `SubmissionsListResponse`, `DaysOpenSeverity`, `SubmissionIdentification`, `SubmissionDates`, `SubmissionRouting`, `SubmissionPremiumDetail`, `SubmissionDecision`, `SubmissionCompliance`, `SubmissionMemberDetail`, `SubmissionBrokerDetail`, `SubmissionBrokerContact`, `SubmissionDetail`, `SubmissionHero`, `CoverageLine`, `LossHistoryRow`, `TaskPriority`, `TaskFilter`, `Task`, `UnderwriterRole`, `TeamPerfStatKey`, `UnderwriterPerformance`, `TeamPerformance`, `TeamPerfColumnKey`

## shared/utils `@/shared/utils`
`formatAlertTimestamp`, `formatDaysToQuote`, `formatHitRatio`, `formatStatValue`, `formatCurrency`, `formatCompactCurrency`, `formatPercent`, `formatDate`, `getInitials`, `niceScale`

